#!/usr/bin/env bash
# Wird per SSM auf den Instanzen ausgeführt (Inhalt der Commands in der Workflow-YAML).
set -euo pipefail
echo "Siehe GitHub Actions deploy-aws.yml für Backend-/Frontend-Deploy-Befehle."
